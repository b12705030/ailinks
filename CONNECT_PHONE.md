# 📱 連接手機到 Android Studio - 完整指南

## 🔌 方法 1：USB 連接（推薦）

### 步驟 1：啟用開發者選項

1. **打開手機設置**
2. **關於手機**（或「關於設備」）
3. **找到「版本號碼」**（或「內部版本號」）
4. **連續點擊 7 次**，直到看到「您已成為開發者」

### 步驟 2：啟用 USB 調試

1. **返回設置主頁**
2. **找到「開發者選項」**（通常在「系統」或「其他設置」中）
3. **打開「開發者選項」**
4. **啟用「USB 調試」**
5. **啟用「USB 安裝」**（如果有）

### 步驟 3：連接手機

1. **用 USB 線連接手機到電腦**
2. **在手機上會彈出提示**：
   - 選擇「允許 USB 調試」
   - **勾選「一律允許這部電腦」**
   - 點擊「確定」

### 步驟 4：檢查連接

在 Android Studio 中：
- 點擊頂部工具欄的設備選擇器（顯示 "No devices" 的地方）
- 應該能看到你的手機型號

如果還是看不到，繼續下面的步驟。

---

## 🔧 方法 2：使用 ADB 檢查連接

### 步驟 1：檢查 ADB 是否識別設備

1. **打開終端/命令提示符**（Windows：`Win + R` → 輸入 `cmd`）
2. **運行命令**：
   ```bash
   adb devices
   ```

### 步驟 2：查看結果

**如果看到設備**：
```
List of devices attached
ABC123XYZ    device
```
✅ 設備已連接！

**如果看到 "unauthorized"**：
```
List of devices attached
ABC123XYZ    unauthorized
```
❌ 需要在手機上允許 USB 調試

**如果沒有設備**：
```
List of devices attached
```
❌ 繼續下面的故障排除

---

## 🛠️ 故障排除

### 問題 1：ADB 找不到設備

**解決方法**：

1. **重新啟動 ADB**：
   ```bash
   adb kill-server
   adb start-server
   adb devices
   ```

2. **檢查 USB 線**：
   - 嘗試不同的 USB 線
   - 確保是數據線（不是只能充電的線）
   - 嘗試不同的 USB 端口

3. **重新連接手機**：
   - 拔掉 USB 線
   - 重新插入
   - 在手機上重新允許 USB 調試

### 問題 2：需要安裝驅動程序（Windows）

**如果使用 Windows 且設備顯示為 "unknown"**：

1. **下載手機驅動程序**：
   - 華為：HiSuite
   - 小米：Mi Mover 或 Mi PC Suite
   - 三星：Samsung USB Driver
   - OPPO/OnePlus：官方 USB 驅動
   - 其他：搜索「[手機品牌] USB 驅動」

2. **安裝驅動程序後重啟電腦**

3. **重新連接手機**

### 問題 3：手機品牌特殊設置

#### 華為/榮耀：
- 需要啟用「僅充電模式下允許 ADB 調試」
- 可能需要關閉「僅充電模式」

#### 小米：
- 需要登錄小米帳號
- 需要啟用「USB 調試（安全設置）」

#### OPPO/OnePlus：
- 需要啟用「USB 調試（安全設置）」

#### 三星：
- 可能需要安裝 Samsung USB Driver

---

## 📲 方法 3：無線連接（不需要 USB 線）

### 步驟 1：確保手機和電腦在同一 Wi-Fi

### 步驟 2：通過 USB 初始連接（只需一次）

1. **用 USB 線連接手機**
2. **在終端運行**：
   ```bash
   adb tcpip 5555
   ```
3. **拔掉 USB 線**

### 步驟 3：無線連接

1. **查看手機 IP 地址**：
   - 設置 → Wi-Fi → 點擊當前網絡 → 查看 IP 地址
   - 例如：`192.168.1.100`

2. **在終端運行**：
   ```bash
   adb connect 192.168.1.100:5555
   ```
   （替換為你的手機 IP）

3. **檢查連接**：
   ```bash
   adb devices
   ```

---

## 📦 方法 4：直接安裝 APK（不需要連接）

如果無法連接手機，可以直接生成 APK 文件安裝：

### 步驟 1：生成 APK

1. **在 Android Studio**：
   - `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
   - 等待構建完成

2. **找到 APK 文件**：
   - 路徑：`app/build/outputs/apk/debug/app-debug.apk`
   - Android Studio 會顯示通知，點擊 "locate" 即可找到

### 步驟 2：傳輸到手機

**方法 A：通過 USB（即使無法調試）**
- 連接手機到電腦
- 在手機上選擇「文件傳輸」模式
- 複製 APK 到手機

**方法 B：通過雲盤**
- 上傳 APK 到 Google Drive / Dropbox / OneDrive
- 在手機上下載

**方法 C：通過藍牙**
- 從電腦發送 APK 到手機

### 步驟 3：在手機上安裝

1. **打開文件管理器**
2. **找到 APK 文件**
3. **點擊安裝**
4. **允許「安裝未知來源應用」**（如果提示）

---

## ✅ 檢查清單

在告訴我問題之前，請確認：

- [ ] 已啟用開發者選項
- [ ] 已啟用 USB 調試
- [ ] 已用 USB 線連接
- [ ] 已在手機上允許 USB 調試
- [ ] 已運行 `adb devices` 檢查
- [ ] 已嘗試不同的 USB 線和端口

---

## 🆘 如果還是無法連接

請告訴我：

1. **手機品牌和型號**
2. **電腦操作系統**（Windows / Mac / Linux）
3. **運行 `adb devices` 的結果**
4. **Android Studio 中顯示什麼**（"No devices" 還是其他？）

這樣我可以提供更具體的解決方案！

